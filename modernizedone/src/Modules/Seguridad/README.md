# Seguridad

Usuarios, roles, permisos y accesos por instalación. Fuente de verdad para autorizaciones.

Incluye resolución de contexto de usuario actual (dev): `ICurrentUserAuthorizationService` mapea identidad a `UsuarioId`, roles e instalaciones usando `DevAuthorization` en configuración.
